##1.the concepts i clicked immediately today are:
 1.the datatypes
 2.converting /casting
    # converting from the string to the float number 
    # converting from the float nummber into an integer
 3.f-string report
    # "f" usage and the reason behind it .
    #f-string report and the epoch count

 4.conditionals statements:
    # if/leif and else
    # good decision when the accuracy and the latency wer good too
    # not fair when the accuracy is good but the latency too slow 
    # need to re-train when the accuracy is not enough
    #the more the latency the slower the model

 5.looping:
    # moving through all the values 
    # using enamurate and printing the epoch N:score

 6.collection:
    #tuple,list ,dictionary and set

##2. the concept which is still fuzzy:
  1. function using
  2. matplotlib figures
  3. pandas and numpay

##3. question i want answered by tomorrow: 
   1. function coming again
   2. usage of numpy and pandas and also matplotlib

##4. hours it took me:
   honestly because there was kind of difficulties and boundaries and also i had to make the research it took all around 5hours+ and im still moving on with the new features im meeting in the journey by having reources from the references eg : those you proided and also contuinue learning....
    
   ~